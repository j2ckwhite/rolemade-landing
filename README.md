# RoleMade — website

Static site. No build step.

- `index.html` — home (hero, pinned four-step journey, jobs, fit, calculator)
- `toolkits.html`, `growth.html`, `about.html`, `start.html`
- `styles.css`, `site.js`, `assets/`

Lead form submit is a placeholder in `site.js` (see TODO) until the quiz/API is wired.
